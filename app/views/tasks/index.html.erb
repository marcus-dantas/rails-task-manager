<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <ul>
    <li>
      <% if task.completed %>
        <input type="checkbox">
      <% else %>

      <%= link_to task["title"], task_path(task) %>
    </li>
  </ul>
<% end %>
<p><%= link_to "New task", "tasks/new" %></p>
